# HTMLstyle屬性用於向元素添加樣式，例如顏色，字體，大小等。
```
例子
<!DOCTYPE html>
<html>
<body>

<p>I am normal</p>
<p style="color:red;">I am red</p>
<p style="color:blue;">I am blue</p>
<p style="font-size:50px;">I am big</p>

</body>
</html>
```
# HTML樣式屬性
```
可以使用style屬性來設置HTML元素的樣式。

HTMLstyle屬性具有以下語法：

<tagname style="property:value;">
```
# 背景顏色
```
CSSbackground-color屬性定義HTML元素的背景色。

例子
將頁面的背景色設置為powderblue：

<body style="background-color:powderblue;">

<h1>This is a heading</h1>
<p>This is a paragraph.</p>

</body>
```
# 文字顏色
```
CSScolor屬性定義HTML元素的文本顏色：

例子
<h1 style="color:blue;">This is a heading</h1>
<p style="color:red;">This is a paragraph.</p>
```
# 字型
```
CSSfont-family屬性定義用於HTML元素的字體：

例子
<h1 style="font-family:verdana;">This is a heading</h1>
<p style="font-family:courier;">This is a paragraph.</p>
```
# 字體大小
```
CSS font-size 屬性定義HTML元素的文本大小：

例子
<h1 style="font-size:300%;">This is a heading</h1>
<p style="font-size:160%;">This is a paragraph.</p>
```
# 文字對齊
```
CSStext-align屬性定義HTML元素的水平文本對齊方式：

例子
<h1 style="text-align:center;">Centered Heading</h1>
<p style="text-align:center;">Centered paragraph.</p>
```
# 章節總結
```
使用該style屬性來樣式化HTML元素
使用background-color的背景顏色
使用color的文字顏色
使用font-family的文字字體
使用font-size文本大小
使用text-align文本對齊
```



























